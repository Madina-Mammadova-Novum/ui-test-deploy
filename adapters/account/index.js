export const requestDeactivateAccountAdapter = ({ data }) => {
  if (!data) return {};
  return data;
};

export const responseDeactivateAccountAdapter = ({ data }) => {
  if (!data) return {};
  return data;
};

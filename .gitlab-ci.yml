image: node
stages:
  - build
cache:
  paths:
    - node_modules/
build-staging:
  stage: build
  only:
    - develop
  tags:
    - staging
  script:
    # ~/builds/sdfsd/sdfsdfsd
    - yarn install
    - cp .env.staging .env
    - yarn turbo-build
    - rm -rf /var/www/web/.next
    - rsync -a . /var/www/web
    - cd /var/www/web
    - pm2 restart ecosystem.config.js

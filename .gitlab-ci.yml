image: node
stages:
  - build
cache:
  paths:
  - node_modules/
build-staging:
  stage: build
  only:
   - master
  tags:
    - staging
  script:
   - npm i
   - npm run build
   - rsync -a --progress . /var/www/web 
   - cd /var/www/web 
   - pm2 restart ecosystem.config.js
const MapLoader = () => {
  return (
    <div className="absolute w-full h-full top-0 left-0 z-[999] flex justify-center items-center bg-black opacity-50">
      <div className="size-10 border-4 rounded-full border-t-blue animate-spin" />
    </div>
  );
};

export default MapLoader;
